version: '3.8'

services:
  webapp:
    image: workshopimage:1.0
    ports:
      - "8080:8080"
    environment:
      CATALINA_BASE: /usr/local/tomcat
      MYSQL_HOST: localhost
      MYSQL_PORT: 3306
      MYSQL_DATABASE: amazon
      MYSQL_USER: root
      MYSQL_PASSWORD: Yadhu
    depends_on:
      - mysql

  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: Yadhu
      MYSQL_DATABASE: amazon
    volumes:
      - ./scripts:/docker-entrypoint-initdb.d
